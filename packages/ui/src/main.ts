import { カンバンビュー } from "./カンバン/カンバンビュー";
import { キャラクライアント } from "./通信/キャラクライアント";
import { 札クライアント } from "./通信/札クライアント";

// コンポジションルート（動作確認ハーネス用のスタンドアロン起動）。依存の生成と配線は
// ここに集約する。本番配線（Jimboシェルへのタブ注入）はホスト側のコンポジションルートが行う
new カンバンビュー(new 札クライアント(), new キャラクライアント()).bodyにマウントする();
