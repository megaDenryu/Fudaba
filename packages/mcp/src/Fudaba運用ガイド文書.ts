// AIエージェントがFudaba（札場）ツールをどう使うべきかの推奨運用パターン。
// MCPリソースやシステムプロンプトへの埋め込み等、配線方法はホスト側に委ねる純粋な文字列定数

export const Fudaba運用ガイド文書 = `
# Fudaba（札場）運用ガイド

Fudabaは人間とAIが同じ作業アイテム「札」を読み書きする台帳。あなたが発見したバグや進めているタスクを
セッション内メモリだけに留めず、ここに記録すると、人間がUIのカンバンビューで同じ状態を見られる。

## 推奨パターン

1. 作業中にバグを発見したら fudaba_create で 種別=バグ として記録する。
2. 依頼されたタスクや自発的に着手した作業は fudaba_create で 種別=タスク として記録する。
3. 作業に着手したら fudaba_update で 状態=進行中 にする。
4. 作業が終わったら fudaba_update で 状態=完了 にする。行き詰まって人間の判断待ちになったら
   状態=ブロック にする。
5. 重要な意思決定や合意事項は 種別=決定 として記録し、状態=完了 で作るのが自然（強制ではない）。
6. 自分が今何をすべきか分からなくなったら fudaba_list で担当者フィルタをかけ、自分名義の
   未完了札を確認する。
7. 状態遷移に制約は無い。どの状態からどの状態へも変更してよい。

人間は同じ札をUIで見ている。あなたが更新した内容は次のポーリングでUIに反映される。
`.trim();
