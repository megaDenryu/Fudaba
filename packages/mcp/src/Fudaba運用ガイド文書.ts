// AIエージェントがFudaba（札場）ツールをどう使うべきかの推奨運用パターン。
// MCPリソースやシステムプロンプトへの埋め込み等、配線方法はホスト側に委ねる純粋な文字列定数

export const Fudaba運用ガイド文書 = `
# Fudaba（札場）運用ガイド

Fudabaは人間とAIが同じ作業アイテム「札」を読み書きする台帳。あなたが発見したバグや進めているタスクを
セッション内メモリだけに留めず、ここに記録すると、人間がUIのカンバンビューで同じ状態を見られる。

## 推奨パターン

1. 作業中にバグを発見したら fudaba_create で 種別=バグ として記録する。
2. 依頼されたタスクや自発的に着手した作業は fudaba_create で 種別=タスク として記録する。
3. 作業に着手したら fudaba_update で 状態=進行中 にする。
4. 札を完了へ移す前に、本文やコメントに「将来」「残課題」「未対応」「次回」等の未来作業が
   残っていないか確認する。残っていれば着手条件と出自を明記した独立札を 状態=未着手 で作り、
   元札と親子リンクで結ぶ。完了札へ未来作業を埋めたままにしない。
5. 作業が終わったら fudaba_update で 状態=完了 にする。行き詰まって人間の判断待ちになったら
   状態=ブロック にする。
6. 重要な意思決定や合意事項は 種別=決定 として記録し、状態=完了 で作るのが自然（強制ではない）。
7. 自分が今何をすべきか分からなくなったら fudaba_list で担当者フィルタをかけ、自分名義の
   未完了札を確認する。
8. 状態遷移に制約は無い。どの状態からどの状態へも変更してよい。
9. 所属が決まらないアイデアはラベルなしで投げてよい。分解するAIが適切なラベル
   (リポジトリ名等: jimbo/fudaba/boomyack/agentroom)を付ける。ラベルは事前登録不要で、
   fudaba_update でいつでも後から付け替えられる(渡した配列で既存ラベルを全置換する)。
10. 人間の判断が必要なら fudaba_question_create で問いを起票し、返された問いIDを保持する。
11. fudaba_question_wait_answers に複数の問いIDを渡して回答を待つ。タイムアウトは正常なので、
    同じafterSeqで再度待つ。回答を受け取ったら最大連番を次回のafterSeqにする。
12. スクリーンショット等の根拠は fudaba_add_attachment で関連札へ追加し、AIが読むときは
    fudaba_listの保存名を fudaba_get_attachment に渡す。

人間は同じ札をUIで見ている。あなたが更新した内容は次のポーリングでUIに反映される。
`.trim();
