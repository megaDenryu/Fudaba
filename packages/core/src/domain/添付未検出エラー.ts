// 指定保存名の添付ファイルが見つからないことを表す型付き例外。APIのエラーハンドラは
// この型を404に写像する（札未検出エラーと同じ方針）
export class 添付未検出エラー extends Error {
  constructor(保存名: string) {
    super(`添付ファイルが見つかりません: 保存名=${保存名}`);
    this.name = "添付未検出エラー";
  }
}
